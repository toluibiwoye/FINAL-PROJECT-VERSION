// Base URL for the API, assuming it's running locally on the emulator.
const baseUrl = "10.0.2.2:3000";

// Endpoint for the login API.
const loginApi = "/api/users/login";

// Endpoint for the register API.
const registerApi = '/register';